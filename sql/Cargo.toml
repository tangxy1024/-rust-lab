[package]
name = "sql"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "sql"
path = "src/main.rs"

[dependencies]
sqlx = { version = "0.8", features = [
    "runtime-tokio",
    "postgres",
    "macros"
] }
tokio = { version = "1", features = ["full"] }